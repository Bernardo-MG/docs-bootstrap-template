language: python
python:
  - "2.7"
env:
  - DEPLOY=true
before_install:
  - chmod +x ./.scripts/deploy.sh
  - PULL_REQUEST = $TRAVIS_PULL_REQUEST
  - CMS_BRANCH = $TRAVIS_BRANCH
install:
  - pip install html5validator
script:
  - html5validator
after_success:
  - ./.scripts/deploy.sh
