language: python
python:
  - "2.7"
install:
  - pip install html5validator
  - sudo apt-get install wput
script:
  - html5validator
after_success:
  - "wput * ftp://$REPO_USER:$REPO_PASSWORD@$REPO_URL"