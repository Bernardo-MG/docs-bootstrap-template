language: python
python:
 - "2.7"
env:
  - DEPLOY=true PULL_REQUEST=$TRAVIS_PULL_REQUEST CMS_BRANCH=$TRAVIS_BRANCH
install:
  - pip install html5validator
  - npm install -g jshint
before_script:
  - chmod +x ./.scripts/deploy.sh
script:
  - html5validator
  - jshint **.*
after_success:
  - ./.scripts/deploy.sh
